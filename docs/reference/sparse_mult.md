# Element-wise Multiplication for sparse_numeric Vectors

Element-wise Multiplication for sparse_numeric Vectors

## Usage

``` r
sparse_mult(x, y)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_mult(x, y)
```

## Arguments

- x:

  A `sparse_numeric` vector.

- y:

  Another `sparse_numeric` vector.

## Value

A `sparse_numeric` object containing element-wise products.

## Methods (by class)

- `sparse_mult(x = sparse_numeric, y = sparse_numeric)`: Multiply two
  sparse vectors.
