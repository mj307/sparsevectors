# Subtract Two sparse_numeric Vectors

Subtract Two sparse_numeric Vectors

## Usage

``` r
sparse_sub(x, y)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_sub(x, y)
```

## Arguments

- x:

  First sparse vector.

- y:

  Second sparse vector to subtract.

## Value

A sparse vector representing `x - y`.

## Methods (by class)

- `sparse_sub(x = sparse_numeric, y = sparse_numeric)`: Subtract two
  sparse vectors.
